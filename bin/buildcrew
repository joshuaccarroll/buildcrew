#!/bin/bash
# BuildCrew - Autonomous AI Development Workflow
# https://github.com/joshuaccarroll/buildcrew

# Wrap entire script in a block so bash parses it fully before executing.
# This prevents syntax errors when cmd_update overwrites this file mid-run.
{

set -e

# Determine script location (follow symlinks)
SOURCE="${BASH_SOURCE[0]}"
while [ -L "$SOURCE" ]; do
    DIR="$(cd -P "$(dirname "$SOURCE")" && pwd)"
    SOURCE="$(readlink "$SOURCE")"
    [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE"
done
SCRIPT_DIR="$(cd -P "$(dirname "$SOURCE")" && pwd)"
BUILDCREW_HOME="$(dirname "$SCRIPT_DIR")"
export BUILDCREW_HOME

# Source library files (common.sh first — provides colors and helpers to all)
source "$BUILDCREW_HOME/lib/common.sh"
source "$BUILDCREW_HOME/lib/version.sh"
source "$BUILDCREW_HOME/lib/update.sh"
source "$BUILDCREW_HOME/lib/plugins.sh"

print_logo() {
    echo -e "${CYAN}"
    echo "  ____        _ _     _  ____                    "
    echo " | __ ) _   _(_) | __| |/ ___|_ __ _____      __ "
    echo " |  _ \\| | | | | |/ _\` | |   | '__/ _ \\ \\ /\\ / / "
    echo " | |_) | |_| | | | (_| | |___| | |  __/\\ V  V /  "
    echo " |____/ \\__,_|_|_|\\__,_|\\____|_|  \\___| \\_/\\_/   "
    echo -e "${NC}"
}

print_help() {
    print_logo
    echo -e "${BOLD}BuildCrew${NC} - Autonomous AI Development Workflow"
    echo ""
    echo -e "${BOLD}USAGE:${NC}"
    echo "    buildcrew <command> [options]"
    echo ""
    echo -e "${BOLD}COMMANDS:${NC}"
    echo "    init             Initialize BuildCrew in current project"
    echo "    run              Execute workflow on BACKLOG.md"
    echo "    status           Show backlog stats and last workflow result"
    echo "    stop             Stop workflow after current task completes"
    echo "    reset            Clear blocked tasks and clean up artifacts"
    echo "    lessons          List recorded lessons"
    echo "    lessons promote  Graduate a lesson to permanent project rules"
    echo "    lessons prune    Interactively remove stale lessons"
    echo "    plugins          Show recommended plugins for your project"
    echo "    update           Check for and install updates"
    echo "    version          Show version information"
    echo "    uninstall        Remove BuildCrew from system"
    echo "    help             Show this help message"
    echo ""
    echo -e "${BOLD}OPTIONS:${NC}"
    echo "    --single      Process only one task (with 'run')"
    echo "    --dry-run     Preview without executing (with 'run')"
    echo "    --review      Pause for human review at plan checkpoints (with 'run')"
    echo "    --branch      Create a feature branch per task with optional PR (with 'run')"
    echo "    --resume      Resume an interrupted task from where it left off (with 'run')"
    echo "    --task N      Target a specific task by name or number (with 'run')"
    echo "    --teams       Use agent teams mode — experimental (with 'run')"
    echo "    --skip-spec   Skip the specification refinement phase (with 'run')"
    echo "    --strict      Require ALL acceptance criteria to pass before commit (with 'run')"
    echo ""
    echo -e "${BOLD}GETTING STARTED:${NC}"
    echo -e "    1. Run ${CYAN}buildcrew init${NC} in your project"
    echo -e "    2. Run ${CYAN}buildcrew run${NC} to start"
    echo -e "    (Auto-launches build mode if no backlog exists)"
    echo ""
    echo -e "${BOLD}DOCUMENTATION:${NC}"
    echo "    https://github.com/joshuaccarroll/buildcrew"
    echo ""
}

cmd_init() {
    echo -e "${BOLD}Initializing BuildCrew in current project...${NC}"
    echo ""

    # Create .claude directory (minimal - uses global installation)
    mkdir -p .claude

    # Create .buildcrew-link marker pointing to global installation
    if [ ! -f ".claude/.buildcrew-link" ]; then
        echo "BUILDCREW_HOME=$BUILDCREW_HOME" > .claude/.buildcrew-link
        echo -e "  ${GREEN}✓${NC} Linked to BuildCrew at $BUILDCREW_HOME"
    else
        echo -e "  ${YELLOW}○${NC} BuildCrew link exists"
    fi

    # Symlink commands from global installation
    if [ ! -e ".claude/commands" ]; then
        ln -s "$BUILDCREW_HOME/commands" ".claude/commands"
        echo -e "  ${GREEN}✓${NC} Linked commands"
    else
        echo -e "  ${YELLOW}○${NC} .claude/commands exists"
    fi

    # Symlink skills from global installation
    if [ ! -e ".claude/skills" ]; then
        ln -s "$BUILDCREW_HOME/skills" ".claude/skills"
        echo -e "  ${GREEN}✓${NC} Linked skills"
    else
        echo -e "  ${YELLOW}○${NC} .claude/skills exists"
    fi

    # Create .buildcrew directory for project-specific overrides
    mkdir -p .buildcrew/rules
    mkdir -p .buildcrew/norms

    # Create workflow.md.example if not present
    if [ ! -f ".buildcrew/workflow.md.example" ]; then
        cat > .buildcrew/workflow.md.example << 'EOF'
# Custom Workflow (Example)

Copy this file to workflow.md to customize your project's workflow.
Delete phases you don't need, or add new ones.

---

## Default Phase Structure (as of v1.11.0)

The default workflow runs these phases in order:

### Phase 0: SPEC (optional)
agent: product-manager
description: Convert raw backlog item into structured spec with testable acceptance criteria
output: .claude/spec.md
skip with: buildcrew run --skip-spec

### Phase 1: RESEARCH + PLAN
agent: none
description: Gather context, create implementation plan
output: .claude/research.md, .claude/current-plan.md

### Phase 2: PLAN_REVIEW (adversarial, 3-pass)
agents: principal-engineer, product-manager
description: Adversarial 3-pass review — find the most serious flaw
output: .claude/plan-review.md
gate: overall verdict == "APPROVED"

### Phase 3: BUILD
agent: feature-engineer
description: Implement the plan
output: working code

### Phase 4: CODE_REVIEW + REFACTOR + TEST
agents: principal-engineer, qa-engineer
description: Adversarial code review + elegance check + test suite
output: .claude/code-review.md, .claude/test-report.md

### Phase 4.5: OUTCOME_VERIFY
agent: qa-engineer
description: Validate each acceptance criterion from the spec against the actual implementation
output: .claude/outcome-report.md
note: Only runs if .claude/spec.md exists (i.e., spec phase was not skipped)
strict mode: buildcrew run --strict (blocks commit if any criteria fail)

### Phase 5: VERIFY + COMMIT
agents: security-engineer, none
description: Security audit + final gate + git commit
output: .claude/security-audit.md, .claude/verify-report.md
gate: all checks pass

---

## Minimal Custom Workflow Example

Paste this into .buildcrew/workflow.md to override the default:

```markdown
## Phases

### Phase 1: PLAN
agent: none
description: Create implementation plan

### Phase 2: BUILD
agent: feature-engineer
description: Implement the feature

### Phase 3: TEST
agent: qa-engineer
description: Write and run tests

### Phase 4: COMMIT
agent: none
description: Create git commit
```

---

## Available Agents

- principal-engineer: Architecture & adversarial code review
- feature-engineer: Pragmatic implementation
- security-engineer: Security audits
- qa-engineer: Testing and outcome verification
- product-manager: Discovery, spec writing, plan review
- ux-designer: Design specs

---

## Customization Tips

1. Use --skip-spec for tasks that already have detailed specs
2. Use --strict to enforce all acceptance criteria before commit
3. Remove PLAN_REVIEW and CODE_REVIEW for faster iteration on simple tasks
4. Add a DEPLOY phase after COMMIT for CI/CD integration
5. Run `buildcrew lessons` to review what BuildCrew has learned from past failures

See ~/.buildcrew/workflows/default-workflow.md for the full default workflow.
EOF
        echo -e "  ${GREEN}✓${NC} Created .buildcrew/workflow.md.example"
    else
        echo -e "  ${YELLOW}○${NC} Workflow example exists"
    fi

    # Create project-rules.md.example if not present
    if [ ! -f ".buildcrew/rules/project-rules.md.example" ]; then
        cat > .buildcrew/rules/project-rules.md.example << 'EOF'
# Project-Specific Rules (Example)

Copy this file to project-rules.md to add custom rules for your project.
These rules extend the core BuildCrew principles.

---

## Override: Functions
<!-- Use ## Override: to replace a default rule -->
- Keep functions under 30 lines (default is 20)

## Extend: Custom Rules
<!-- Use ## Extend: to add to existing rules -->

### Linting & Formatting
- Run `npm run lint` before committing
- All TypeScript files must pass strict mode
- Use Prettier with project config

### Naming Conventions
- React components: PascalCase (UserProfile.tsx)
- Utilities: camelCase (formatDate.ts)
- Constants: SCREAMING_SNAKE_CASE
- Database tables: snake_case

### API Design
- All endpoints must return { data, error, meta }
- Use plural nouns for resource collections
- Version all public APIs: /v1/users

### Operational Rules
- API calls must use exponential backoff
- Batch updates to prevent thundering herd
- Rate limit external API calls to 10/sec

### Database
- All queries must use parameterized statements
- No N+1 query patterns
- Index foreign keys
- Use transactions for multi-step operations

### Frontend
- Components must be under 200 lines
- State management through designated store only
- All interactive elements must be keyboard accessible

## Disable: YAGNI
<!-- Use ## Disable: to remove a rule section entirely -->
<!-- Uncomment to disable YAGNI enforcement -->
EOF
        echo -e "  ${GREEN}✓${NC} Created .buildcrew/rules/project-rules.md.example"
    else
        echo -e "  ${YELLOW}○${NC} Rules example exists"
    fi

    # Create context directory and example files
    mkdir -p .buildcrew/context

    if [ ! -f ".buildcrew/context/users.md.example" ]; then
        cat > .buildcrew/context/users.md.example << 'EOF'
# User Context

Copy this file to `users.md` to provide user context to BuildCrew.
This helps the Product Manager and plan reviewers understand who you're building for.

---

## Primary User

- **Who**: [e.g., Frontend developers on small teams]
- **Needs**: [e.g., Ship features fast without sacrificing quality]
- **Workflow**: [e.g., Uses VS Code, runs CI locally, deploys via GitHub Actions]
- **Success looks like**: [e.g., Feature works on first deploy, no rollbacks]

## Secondary User

- **Who**: [e.g., Team leads reviewing PRs]
- **Needs**: [e.g., Understand changes quickly, trust the test suite]
- **Workflow**: [e.g., Reviews PRs on GitHub, checks CI status]
- **Success looks like**: [e.g., PR description is clear, tests cover edge cases]

## Non-Users / Out of Scope

- [e.g., End users of the product — they interact through the UI, not the codebase]
EOF
        echo -e "  ${GREEN}✓${NC} Created .buildcrew/context/users.md.example"
    else
        echo -e "  ${YELLOW}○${NC} Users context example exists"
    fi

    if [ ! -f ".buildcrew/context/principles.md.example" ]; then
        cat > .buildcrew/context/principles.md.example << 'EOF'
# Project Principles

Copy this file to `principles.md` to guide BuildCrew's decisions.
When tradeoffs arise, these principles determine which direction to take.

---

## Principles

- **Ship over polish**: Working software in users' hands beats perfect code in a branch. Cut scope before delaying delivery.
- **Simplicity over power**: Choose the boring solution. A simple approach that works is better than a clever one that might.
- **Explicit over implicit**: Name things clearly, avoid magic, make dependencies obvious. Future-you is a stranger.
- **Fix the root cause**: Don't patch symptoms. If something keeps breaking, redesign it.
EOF
        echo -e "  ${GREEN}✓${NC} Created .buildcrew/context/principles.md.example"
    else
        echo -e "  ${YELLOW}○${NC} Principles context example exists"
    fi

    if [ ! -f ".buildcrew/context/domain.md.example" ]; then
        cat > .buildcrew/context/domain.md.example << 'EOF'
# Domain Knowledge

Copy this file to `domain.md` to give BuildCrew context about your problem domain.
This helps personas make informed decisions without asking repetitive questions.

---

## Terminology

| Term | Definition |
|------|-----------|
| [Term] | [What it means in your project] |

## Business Rules

- [e.g., Users must verify email before accessing paid features]
- [e.g., All prices are stored in cents to avoid floating-point issues]

## Key Constraints

- [e.g., Must support IE11 for enterprise clients]
- [e.g., API responses must be < 200ms at p95]
- [e.g., HIPAA compliance required — no PII in logs]
EOF
        echo -e "  ${GREEN}✓${NC} Created .buildcrew/context/domain.md.example"
    else
        echo -e "  ${YELLOW}○${NC} Domain context example exists"
    fi

    # Create BACKLOG.md template if not present
    if [ ! -f "BACKLOG.md" ]; then
        cat > BACKLOG.md << 'EOF'
# Backlog

*Add your tasks here, then run `buildcrew run` to process them.*

## High Priority

- [ ] Your first task here

## Medium Priority

- [ ] Another task

## Low Priority

- [ ] Future task

---
*Use `/build` in Claude Code to generate a comprehensive backlog from your project plan.*
EOF
        echo -e "  ${GREEN}✓${NC} Created BACKLOG.md template"
    else
        echo -e "  ${YELLOW}○${NC} BACKLOG.md exists"
    fi

    # Create or update .claude/settings.json with comprehensive autonomous permissions
    if [ ! -f ".claude/settings.json" ]; then
        cat > .claude/settings.json << 'EOF'
{
  "permissions": {
    "allow": [
      "Read",
      "Write",
      "Edit",
      "Glob",
      "Grep",
      "TodoWrite",
      "WebFetch",
      "WebSearch",

      "Bash(npm:*)",
      "Bash(npx:*)",
      "Bash(yarn:*)",
      "Bash(pnpm:*)",
      "Bash(bun:*)",
      "Bash(node:*)",
      "Bash(deno:*)",

      "Bash(pip:*)",
      "Bash(pip3:*)",
      "Bash(python:*)",
      "Bash(python3:*)",
      "Bash(poetry:*)",
      "Bash(uv:*)",
      "Bash(pytest:*)",
      "Bash(ruff:*)",
      "Bash(black:*)",
      "Bash(mypy:*)",
      "Bash(flake8:*)",

      "Bash(go:*)",
      "Bash(cargo:*)",
      "Bash(rustc:*)",
      "Bash(rustfmt:*)",
      "Bash(clippy:*)",

      "Bash(mvn:*)",
      "Bash(gradle:*)",
      "Bash(ant:*)",

      "Bash(dotnet:*)",
      "Bash(nuget:*)",

      "Bash(ruby:*)",
      "Bash(bundle:*)",
      "Bash(gem:*)",
      "Bash(rake:*)",
      "Bash(rails:*)",

      "Bash(php:*)",
      "Bash(composer:*)",
      "Bash(artisan:*)",

      "Bash(swift:*)",
      "Bash(xcodebuild:*)",

      "Bash(make:*)",
      "Bash(cmake:*)",
      "Bash(ninja:*)",
      "Bash(gcc:*)",
      "Bash(g++:*)",
      "Bash(clang:*)",

      "Bash(docker:*)",
      "Bash(docker-compose:*)",
      "Bash(podman:*)",

      "Bash(kubectl:*)",
      "Bash(helm:*)",
      "Bash(terraform:*)",
      "Bash(pulumi:*)",

      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git branch:*)",
      "Bash(git checkout:*)",
      "Bash(git switch:*)",
      "Bash(git merge:*)",
      "Bash(git rebase:*)",
      "Bash(git stash:*)",
      "Bash(git fetch:*)",
      "Bash(git pull:*)",
      "Bash(git push:*)",
      "Bash(git tag:*)",
      "Bash(git remote:*)",
      "Bash(git cherry-pick:*)",
      "Bash(git rev-parse:*)",
      "Bash(git ls-files:*)",
      "Bash(git worktree:*)",

      "Bash(ls:*)",
      "Bash(cat:*)",
      "Bash(head:*)",
      "Bash(tail:*)",
      "Bash(less:*)",
      "Bash(more:*)",
      "Bash(wc:*)",
      "Bash(sort:*)",
      "Bash(uniq:*)",
      "Bash(diff:*)",
      "Bash(comm:*)",
      "Bash(cut:*)",
      "Bash(tr:*)",
      "Bash(awk:*)",
      "Bash(sed:*)",
      "Bash(grep:*)",
      "Bash(egrep:*)",
      "Bash(fgrep:*)",
      "Bash(rg:*)",
      "Bash(ag:*)",
      "Bash(find:*)",
      "Bash(fd:*)",
      "Bash(locate:*)",
      "Bash(which:*)",
      "Bash(whereis:*)",
      "Bash(type:*)",
      "Bash(file:*)",
      "Bash(stat:*)",
      "Bash(du:*)",
      "Bash(df:*)",

      "Bash(mkdir:*)",
      "Bash(touch:*)",
      "Bash(cp:*)",
      "Bash(mv:*)",
      "Bash(rm:*)",
      "Bash(rmdir:*)",
      "Bash(ln:*)",
      "Bash(chmod:*)",

      "Bash(echo:*)",
      "Bash(printf:*)",
      "Bash(tee:*)",
      "Bash(xargs:*)",
      "Bash(env:*)",
      "Bash(printenv:*)",
      "Bash(export:*)",
      "Bash(source:*)",
      "Bash(date:*)",
      "Bash(time:*)",
      "Bash(sleep:*)",
      "Bash(true:*)",
      "Bash(false:*)",
      "Bash(test:*)",
      "Bash([:*)",

      "Bash(curl:*)",
      "Bash(wget:*)",
      "Bash(http:*)",
      "Bash(jq:*)",
      "Bash(yq:*)",
      "Bash(xmllint:*)",

      "Bash(tar:*)",
      "Bash(zip:*)",
      "Bash(unzip:*)",
      "Bash(gzip:*)",
      "Bash(gunzip:*)",
      "Bash(bzip2:*)",
      "Bash(xz:*)",

      "Bash(openssl:*)",
      "Bash(base64:*)",
      "Bash(md5:*)",
      "Bash(sha256sum:*)",
      "Bash(shasum:*)",

      "Bash(ps:*)",
      "Bash(top:*)",
      "Bash(htop:*)",
      "Bash(kill:*)",
      "Bash(pkill:*)",
      "Bash(lsof:*)",
      "Bash(netstat:*)",
      "Bash(ss:*)",

      "Bash(gh:*)",

      "Bash(./test.sh:*)",
      "Bash(./build.sh:*)",
      "Bash(./run.sh:*)",
      "Bash(./start.sh:*)",
      "Bash(./deploy.sh:*)",
      "Bash(./setup.sh:*)",
      "Bash(./scripts/*:*)",
      "Bash(bats:*)"
    ],
    "deny": [
      "Bash(sudo:*)",
      "Bash(su:*)",
      "Bash(doas:*)",

      "Bash(rm -rf /:*)",
      "Bash(rm -rf ~:*)",
      "Bash(rm -rf /*:*)",
      "Bash(rm -rf $HOME:*)",
      "Bash(rm -rf /etc:*)",
      "Bash(rm -rf /usr:*)",
      "Bash(rm -rf /var:*)",
      "Bash(rm -rf /bin:*)",
      "Bash(rm -rf /sbin:*)",
      "Bash(rm -rf /lib:*)",
      "Bash(rm -rf /boot:*)",
      "Bash(rm -rf /root:*)",
      "Bash(rm -rf /System:*)",
      "Bash(rm -rf /Applications:*)",
      "Bash(rm -rf /Library:*)",

      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(git push origin --force:*)",
      "Bash(git push origin -f:*)",
      "Bash(git reset --hard HEAD~:*)",
      "Bash(git reset --hard origin:*)",
      "Bash(git clean -fdx:*)",
      "Bash(git clean -fd:*)",

      "Bash(mkfs:*)",
      "Bash(dd if=/dev/zero:*)",
      "Bash(dd of=/dev:*)",
      "Bash(> /dev/sda:*)",
      "Bash(chmod -R 777 /:*)",
      "Bash(chown -R:*)",

      "Bash(curl | sh:*)",
      "Bash(curl | bash:*)",
      "Bash(wget | sh:*)",
      "Bash(wget | bash:*)",

      "Bash(shutdown:*)",
      "Bash(reboot:*)",
      "Bash(halt:*)",
      "Bash(poweroff:*)",
      "Bash(init:*)",
      "Bash(systemctl:*)",
      "Bash(service:*)",
      "Bash(launchctl:*)",

      "Bash(ssh:*)",
      "Bash(scp:*)",
      "Bash(rsync:*)",

      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(**/*.pem)",
      "Read(**/*.key)",
      "Read(**/id_rsa*)",
      "Read(**/id_ed25519*)",
      "Read(**/.ssh/*)",
      "Read(**/credentials*)",
      "Read(**/secrets*)",
      "Read(**/.aws/*)",
      "Read(**/.gcloud/*)",
      "Read(**/.azure/*)"
    ]
  }
}
EOF
        echo -e "  ${GREEN}✓${NC} Created .claude/settings.json with autonomous permissions"
        echo -e "  ${YELLOW}⚠${NC}  Review .claude/settings.json - these permissions enable autonomous operation"
    else
        echo -e "  ${YELLOW}○${NC} .claude/settings.json exists"
    fi

    echo ""
    echo -e "${GREEN}${BOLD}BuildCrew initialized!${NC}"
    echo ""
    echo -e "${BOLD}What was created:${NC}"
    echo -e "  • ${CYAN}.claude/commands${NC} - BuildCrew slash commands (/build, /buildcrew)"
    echo -e "  • ${CYAN}.claude/skills${NC} - BuildCrew skills (builder, personas)"
    echo -e "  • ${CYAN}.claude/settings.json${NC} - Permissions for Claude Code"
    echo -e "  • ${CYAN}.buildcrew/${NC} - Directory for project-specific customization"
    echo -e "  • ${CYAN}.buildcrew/norms/${NC} - Auto-generated codebase conventions (run /buildcrew norms)"
    echo -e "  • ${CYAN}BACKLOG.md${NC} - Your task backlog"
    echo ""
    echo -e "${BOLD}Optional customization:${NC}"
    echo -e "  • Copy ${CYAN}.buildcrew/workflow.md.example${NC} to ${CYAN}workflow.md${NC} to customize phases"
    echo -e "  • Copy ${CYAN}.buildcrew/rules/project-rules.md.example${NC} to add project rules"
    echo -e "  ${CYAN}Tip:${NC} Add .buildcrew/norms/ to .gitignore for per-dev norms, or commit for shared team norms"
    echo ""
    echo -e "${BOLD}Next steps:${NC}"
    echo -e "  1. Run ${CYAN}buildcrew run${NC} to start"
    echo -e "  2. Use ${CYAN}/buildcrew <persona>${NC} to invoke a persona ad-hoc"
    echo -e "  3. Use ${CYAN}/buildcrew norms${NC} to analyze existing codebase conventions"
    echo ""
}

cmd_plugins() {
    print_logo
    echo -e "${BOLD}Plugin Recommendations${NC}"
    echo ""
    check_plugins
}

cmd_lessons() {
    local subcommand="${1:-}"
    local lessons_file=".buildcrew/lessons.md"

    if [[ ! -f "$lessons_file" ]]; then
        if [[ -z "$subcommand" ]]; then
            echo -e "${CYAN}No lessons recorded yet.${NC}"
            echo "Lessons are automatically written when BuildCrew encounters and resolves failures."
            return
        fi
    fi

    case "$subcommand" in
        "")
            # List all lessons
            echo -e "${BOLD}BuildCrew Lessons${NC}"
            echo ""
            if [[ ! -f "$lessons_file" ]]; then
                echo -e "${CYAN}No lessons recorded yet.${NC}"
                return
            fi

            # Check once for condensed patterns section — used by both display block and total line
            local has_patterns=false
            if grep -q '^## Patterns' "$lessons_file" 2>/dev/null; then
                has_patterns=true
            fi

            # Display condensed patterns section if present
            if [[ "$has_patterns" == "true" ]]; then
                echo -e "  ${BOLD}Condensed Patterns${NC} (older lessons summarized)"
                echo ""
                # Print bullet rules from the Patterns section
                awk '/^## Patterns/,/^## Lesson [0-9]/{
                    if (/^- /) print "  " $0
                }' "$lessons_file"
                echo ""
                echo -e "  --- Active Lessons ---"
                echo ""
            fi

            # Extract and display lesson headers with their phase and rule
            local lesson_count=0
            local in_lesson=false
            local current_num=""
            local current_phase=""
            local current_rule=""
            while IFS= read -r line; do
                if [[ "$line" =~ ^##\ Lesson\ ([0-9]+): ]]; then
                    in_lesson=true
                    current_num="${BASH_REMATCH[1]}"
                    current_phase=""
                    current_rule=""
                    ((lesson_count++))
                elif [[ "$in_lesson" == "true" ]]; then
                    if [[ "$line" =~ ^\*\*Phase\*\*:\ (.+) ]]; then
                        current_phase="${BASH_REMATCH[1]}"
                    elif [[ "$line" =~ ^\*\*Rule\*\*:\ (.+) ]]; then
                        current_rule="${BASH_REMATCH[1]}"
                    elif [[ "$line" == "---" ]]; then
                        echo -e "  ${BOLD}[$current_num]${NC} [${CYAN}${current_phase}${NC}] $current_rule"
                        in_lesson=false
                    fi
                fi
            done < "$lessons_file"
            echo ""
            if [[ "$has_patterns" == "true" ]]; then
                echo -e "  Total: ${lesson_count} lesson(s) (plus older entries condensed into Patterns section above)"
            else
                echo -e "  Total: ${lesson_count} lesson(s)"
            fi
            echo ""
            echo -e "  ${BOLD}buildcrew lessons promote N${NC}   Graduate lesson N to project rules"
            echo -e "  ${BOLD}buildcrew lessons prune${NC}        Interactively remove stale lessons"
            ;;

        promote)
            local lesson_num="${2:-}"
            if [[ -z "$lesson_num" ]] || ! [[ "$lesson_num" =~ ^[0-9]+$ ]]; then
                echo -e "${RED}Error:${NC} Please provide a lesson number to promote."
                echo "Usage: buildcrew lessons promote <number>"
                exit 1
            fi
            if [[ ! -f "$lessons_file" ]]; then
                echo -e "${RED}Error:${NC} No lessons file found."
                exit 1
            fi

            # Extract the lesson
            local lesson_rule=""
            local lesson_phase=""
            local in_target=false
            while IFS= read -r line; do
                if [[ "$line" =~ ^##\ Lesson\ ${lesson_num}: ]]; then
                    in_target=true
                elif [[ "$in_target" == "true" ]]; then
                    if [[ "$line" =~ ^\*\*Phase\*\*:\ (.+) ]]; then
                        lesson_phase="${BASH_REMATCH[1]}"
                    elif [[ "$line" =~ ^\*\*Rule\*\*:\ (.+) ]]; then
                        lesson_rule="${BASH_REMATCH[1]}"
                    elif [[ "$line" == "---" ]]; then
                        break
                    fi
                fi
            done < "$lessons_file"

            if [[ -z "$lesson_rule" ]]; then
                echo -e "${RED}Error:${NC} Lesson $lesson_num not found."
                exit 1
            fi

            # Append to project-rules.md
            local rules_file=".buildcrew/rules/project-rules.md"
            mkdir -p .buildcrew/rules
            if [[ ! -f "$rules_file" ]]; then
                cat > "$rules_file" << 'RULES_HEADER'
# Project Rules

Rules promoted from BuildCrew lessons. These become permanent team standards.

---

RULES_HEADER
            fi

            cat >> "$rules_file" << EOF

## Lesson-Promoted Rule (from Lesson ${lesson_num} — ${lesson_phase})
- ${lesson_rule}
EOF
            echo -e "${GREEN}✓${NC} Lesson $lesson_num promoted to $rules_file"
            echo "  Rule: $lesson_rule"
            ;;

        prune)
            if [[ ! -f "$lessons_file" ]]; then
                echo -e "${CYAN}No lessons to prune.${NC}"
                return
            fi
            if [[ ! -t 0 ]]; then
                echo -e "${YELLOW}⚠${NC} Non-interactive terminal — cannot prune interactively."
                exit 1
            fi

            echo -e "${BOLD}Pruning Lessons${NC} (press Enter to keep, 'd' to delete)"
            echo ""

            local tmp_file
            tmp_file=$(mktemp)
            local in_lesson=false
            local current_block=""
            local current_num=""
            local kept=0
            local pruned=0

            # Copy the header (before first lesson)
            awk '/^## Lesson [0-9]/{exit} {print}' "$lessons_file" > "$tmp_file"

            # Process each lesson block
            while IFS= read -r line; do
                if [[ "$line" =~ ^##\ Lesson\ ([0-9]+): ]]; then
                    current_num="${BASH_REMATCH[1]}"
                    current_block="$line"$'\n'
                    in_lesson=true
                elif [[ "$in_lesson" == "true" ]]; then
                    current_block+="$line"$'\n'
                    if [[ "$line" == "---" ]]; then
                        # Show the lesson and ask
                        echo "$current_block"
                        echo -n "  Keep lesson $current_num? [Enter=keep, d=delete]: "
                        read -r prune_response
                        if [[ "$prune_response" == "d" || "$prune_response" == "D" ]]; then
                            echo -e "  ${YELLOW}Deleted${NC} lesson $current_num"
                            ((pruned++))
                        else
                            echo "$current_block" >> "$tmp_file"
                            ((kept++))
                        fi
                        in_lesson=false
                        current_block=""
                        echo ""
                    fi
                fi
            done < "$lessons_file"

            mv "$tmp_file" "$lessons_file"
            echo -e "${GREEN}✓${NC} Pruning complete: kept $kept, removed $pruned lesson(s)"
            ;;

        *)
            echo -e "${RED}Error:${NC} Unknown lessons subcommand '$subcommand'"
            echo "Usage: buildcrew lessons [promote <N> | prune]"
            exit 1
            ;;
    esac
}

cmd_stop() {
    mkdir -p .buildcrew
    touch .buildcrew/.stop-workflow
    echo -e "${GREEN}✓${NC} Stop signal sent. Workflow will stop after the current task completes."
}

cmd_reset() {
    source "$BUILDCREW_HOME/lib/common.sh"

    # Require BACKLOG.md
    if [[ ! -f "$BACKLOG_FILE" ]]; then
        echo -e "${RED}Error:${NC} No BACKLOG.md found in current directory."
        exit 1
    fi

    echo -e "${BOLD}BuildCrew Reset${NC}"
    echo ""

    local blocked_count
    blocked_count=$(count_tasks blocked)

    # 1. Reset blocked tasks → pending (strip blocked marker and reason)
    if [[ "$blocked_count" -gt 0 ]]; then
        # [!] Task name (blocked: reason) → [ ] Task name
        perl -i -pe 's/^- \[!\] (.+?) \(blocked:.*?\)$/- [ ] $1/' "$BACKLOG_FILE"
        # Catch any [!] lines without a reason suffix
        perl -i -pe 's/^- \[!\] /- [ ] /' "$BACKLOG_FILE"
        echo -e "  ${GREEN}✓${NC} Reset $blocked_count blocked task(s) to pending"
    else
        echo -e "  ${CYAN}○${NC} No blocked tasks to reset"
    fi

    # 2. Clean .claude/ artifacts
    local artifact_files=(
        .claude/research.md .claude/current-plan.md .claude/plan-review.md
        .claude/code-review.md .claude/test-report.md .claude/security-audit.md
        .claude/verify-report.md .claude/current-test-plan.md
        .claude/phase-result.json .claude/workflow-status.json
    )
    local cleaned=0
    for f in "${artifact_files[@]}"; do
        if [[ -f "$f" ]]; then
            rm -f "$f"
            ((cleaned++))
        fi
    done
    if [[ "$cleaned" -gt 0 ]]; then
        echo -e "  ${GREEN}✓${NC} Removed $cleaned artifact file(s)"
    else
        echo -e "  ${CYAN}○${NC} No artifacts to clean"
    fi

    # 3. Remove stale lockfile and state files
    local state_cleaned=0
    for f in .buildcrew/.workflow-lock .buildcrew/task-progress.json .buildcrew/.current-task .buildcrew/.stop-workflow; do
        if [[ -f "$f" ]]; then
            rm -f "$f"
            ((state_cleaned++))
        fi
    done
    if [[ "$state_cleaned" -gt 0 ]]; then
        echo -e "  ${GREEN}✓${NC} Removed $state_cleaned state file(s)"
    else
        echo -e "  ${CYAN}○${NC} No stale state files"
    fi

    echo ""
    echo -e "${GREEN}${BOLD}Reset complete.${NC}"
    echo ""
    echo -e "  Pending: $(count_tasks pending) | Completed: $(count_tasks completed) | Blocked: $(count_tasks blocked)"
    echo ""
}

cmd_status() {
    local version
    version=$(get_version)
    echo -e "${BOLD}BuildCrew${NC} v$version"
    echo ""

    if [[ -f "$BACKLOG_FILE" ]]; then
        echo -e "${BOLD}Backlog:${NC}"
        echo "  Pending:   $(count_tasks pending)"
        echo "  Completed: $(count_tasks completed)"
        echo "  Blocked:   $(count_tasks blocked)"
    else
        echo -e "${YELLOW}No BACKLOG.md found.${NC} Run ${CYAN}buildcrew init${NC} to get started."
    fi

    echo ""
    if [[ -f "$STATUS_FILE" ]]; then
        echo -e "${BOLD}Last workflow result:${NC}"
        if parse_status_file "$STATUS_FILE"; then
            case "$__STATUS_RESULT" in
                complete)
                    echo -e "  Status:  ${GREEN}complete${NC}"
                    echo -e "  Summary: $__STATUS_SUMMARY"
                    ;;
                blocked)
                    echo -e "  Status:  ${YELLOW}blocked${NC}"
                    echo -e "  Reason:  $__STATUS_REASON"
                    ;;
            esac
        else
            echo -e "  ${RED}$__STATUS_REASON${NC}"
        fi
    else
        echo -e "${CYAN}No previous workflow run found.${NC}"
    fi
}

cmd_run() {
    # Check for updates (non-blocking)
    check_for_updates &

    # Verify we're in a project with BuildCrew initialized
    if [ ! -f "BACKLOG.md" ]; then
        echo -e "${RED}Error:${NC} No BACKLOG.md found in current directory."
        echo ""
        echo "Either:"
        echo "  1. Run ${CYAN}buildcrew init${NC} to initialize BuildCrew"
        echo "  2. Create a BACKLOG.md file with tasks"
        echo "  3. Use ${CYAN}/build${NC} in Claude Code to generate one"
        exit 1
    fi

    # Show plugin tip on first run
    if ! plugins_already_checked; then
        show_plugin_tip
    fi

    # Pass through to workflow script
    exec "$BUILDCREW_HOME/lib/workflow.sh" "$@"
}

cmd_update() {
    echo -e "${BOLD}Checking for updates...${NC}"
    echo ""

    local current=$(get_version)
    local latest=$(fetch_latest_version)

    if [[ ! "$latest" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
        echo -e "${RED}Error:${NC} Could not fetch latest version. Check your internet connection."
        exit 1
    fi

    if [ "$current" = "$latest" ]; then
        echo -e "${GREEN}✓${NC} BuildCrew is up to date (v$current)"
        exit 0
    fi

    if ! version_lt "$current" "$latest"; then
        echo -e "${GREEN}✓${NC} BuildCrew is up to date (v$current)"
        exit 0
    fi

    echo -e "Update available: ${YELLOW}v$current${NC} → ${GREEN}v$latest${NC}"
    echo ""

    read -p "Install update? [y/N] " -n 1 -r
    echo ""

    if [[ $REPLY =~ ^[Yy]$ ]]; then
        echo ""
        echo "Downloading v$latest..."

        local tmp_dir=$(mktemp -d)
        local archive="$tmp_dir/buildcrew.tar.gz"

        curl -fsSL "https://github.com/joshuaccarroll/buildcrew/archive/refs/tags/v$latest.tar.gz" -o "$archive"

        echo "Installing..."
        tar -xzf "$archive" -C "$tmp_dir"

        # Run the installer from the new version
        "$tmp_dir/buildcrew-$latest/install.sh" --upgrade

        rm -rf "$tmp_dir"

        echo ""
        echo -e "${GREEN}✓${NC} Updated to v$latest"

        # Exit immediately — install.sh replaced the script on disk,
        # so bash must not read further from the now-stale file descriptor.
        exit 0
    else
        echo "Update cancelled."
    fi
}

cmd_version() {
    local version=$(get_version)
    echo -e "${BOLD}BuildCrew${NC} v$version"
    echo "https://github.com/joshuaccarroll/buildcrew"
}

cmd_uninstall() {
    echo -e "${BOLD}Uninstalling BuildCrew...${NC}"
    echo ""
    echo "This will remove:"
    echo "  - ~/.buildcrew/"
    echo "  - ~/.local/bin/buildcrew symlink"
    echo ""
    echo -e "${YELLOW}Note:${NC} Project files (.claude/, BACKLOG.md, etc.) will NOT be removed."
    echo ""

    read -p "Continue? [y/N] " -n 1 -r
    echo ""

    if [[ $REPLY =~ ^[Yy]$ ]]; then
        rm -rf "$HOME/.buildcrew"
        rm -f "$HOME/.local/bin/buildcrew"
        rm -f "/usr/local/bin/buildcrew" 2>/dev/null || true

        echo ""
        echo -e "${GREEN}✓${NC} BuildCrew has been uninstalled."
        echo ""
        echo -e "${YELLOW}Note:${NC} If you added BuildCrew to your PATH, remove this line from your shell config:"
        echo -e "  ${CYAN}export PATH=\"\$HOME/.local/bin:\$PATH\"${NC}"
        echo ""
        echo "Common locations: ~/.bashrc, ~/.zshrc, ~/.profile, ~/.bash_profile"
        echo ""
        echo "To reinstall, run:"
        echo "  curl -fsSL https://raw.githubusercontent.com/joshuaccarroll/buildcrew/main/install.sh | bash"
    else
        echo "Uninstall cancelled."
    fi
}

# Main command dispatch
case "${1:-}" in
    init)
        shift
        cmd_init "$@"
        ;;
    run)
        shift
        cmd_run "$@"
        ;;
    status)
        cmd_status
        ;;
    stop)
        cmd_stop
        ;;
    reset)
        cmd_reset
        ;;
    lessons)
        shift
        cmd_lessons "$@"
        ;;
    plugins)
        shift
        cmd_plugins "$@"
        ;;
    update)
        shift
        cmd_update "$@"
        ;;
    version|--version|-v)
        cmd_version
        ;;
    uninstall)
        cmd_uninstall
        ;;
    help|--help|-h|"")
        print_help
        ;;
    *)
        echo -e "${RED}Error:${NC} Unknown command '$1'"
        echo ""
        echo "Run 'buildcrew help' for usage information."
        exit 1
        ;;
esac
}
